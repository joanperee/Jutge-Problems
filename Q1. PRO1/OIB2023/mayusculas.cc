#include <iostream>
#include <string>
using namespace std;

string string_to_mayus(string s)
{
    for (int i = 0; i < s.size(); ++i)
    {
        if (int(s[i]) >)
    }
}

int main()
{
    int n;
    cin >> n;
    for (int i = 0; i < n; ++i)
    {
        string s;
        cin >> s;
        cout << string_to_mayus(s) << endl;
    }
}